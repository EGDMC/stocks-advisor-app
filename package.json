{
  "name": "stocks-advisor-app",
  "version": "1.0.0",
  "description": "Stock market analysis application",
  "scripts": {
    "build": "cd netlify/functions && npm install && bash build.sh",
    "start": "python src/app.py",
    "test": "python -m pytest"
  },
  "dependencies": {
    "@types/node": "^16.11.7",
    "child_process": "^1.0.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "python": "3.9.18"
  }
}